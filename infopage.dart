class InfoPage {
  String pid, location, state, description, latitude, longitude, url, contact, address, imagename;

  InfoPage(
      {this.pid,
      this.location,
      this.state,
      this.description,
      this.latitude,
      this.longitude,
      this.url,
      this.contact,
      this.address,
      this.imagename
      });
}